# AGENTS.md

## 1. 문서 목적

이 파일은 이 프로젝트에서 작업하는 AI agent의 **행동 지침**만 정의한다. 프로젝트의 내용과 현재 작업 현황은 `README.md`, 단계별 작업 순서와 진행도는 `TODO_LIST.md`를 따른다.

모든 작업을 시작할 때 아래 세 문서를 생략하지 않고 반드시 다음 순서대로 읽는다.

**필수 열람 순서: `AGENTS.md → README.md → TODO_LIST.md`**

1. `AGENTS.md`: 작업자가 따라야 할 행동 원칙
2. `README.md`: 프로젝트 목적, 범위, 구조, 요구사항과 현재 작업·구현 현황
3. `TODO_LIST.md`: 단계별 작업 순서, 세부 진행도, 다음 작업과 완료 조건

세 문서를 모두 확인하기 전에는 분석을 확정하거나 구현·수정을 시작하지 않는다. 작업은 `README.md`에 기록된 현재 상태를 기준선으로 삼고, `TODO_LIST.md`에서 해당 단계의 완료·미완료·보류 상태를 확인한 뒤 진행한다. 사용자가 새로운 작업을 요청하면 현재 상태와 TODO 순서에 미치는 영향을 먼저 확인한다.

최신 사용자의 명시적 지시가 이 문서와 충돌하면 사용자 지시를 우선한다.

## 2. 계획 및 사전 분석 규칙

코드를 작성하거나 파일을 수정하기 전에 다음 행동을 수행한다.

- 요청된 작업의 목적과 완료 기준을 먼저 정의한다.
- 현재 workspace의 관련 파일과 디렉터리를 조사한다.
- 기존 구현과 문서를 이해한 뒤 변경을 제안한다.
- 영향을 받는 module, interface, 설정, test와 문서를 식별한다.
- 같은 기능이나 유사한 구현이 있는지 project 전체를 검색한다.
- 기존 module과 문서를 우선 재사용하고 중복 기능을 만들지 않는다.
- 큰 변경 전에는 구현 계획과 영향 범위를 사용자에게 설명한다.
- 확인할 수 없는 정보가 결과나 안전에 중대한 영향을 주면 임의로 정하지 않는다.

workspace, 기존 구현, 프로젝트 문서와 사용자가 제공한 요구사항만을 source of truth로 사용한다. 추측이 필요한 항목은 확인되지 않은 사실로 분명히 구분한다.

## 3. 구현 행동 규칙

- 요청된 작업에 필요한 파일만 수정한다.
- 명시적으로 요청받지 않은 관련 없는 refactoring을 하지 않는다.
- 기존 동작과 public interface를 가능한 한 보존한다.
- 기존 project 구조, architecture와 coding style을 따른다.
- 새로운 pattern이나 framework보다 기존 구현과의 일관성을 우선한다.
- 불필요한 dependency와 구조적 복잡성을 추가하지 않는다.
- 기능과 책임이 같은 code를 여러 위치에 중복 구현하지 않는다.
- hardware나 외부 시스템이 준비되지 않았다면 mock 또는 simulator 경계를 사용하고 실제 연동이 완료된 것처럼 표현하지 않는다.
- 안전 관련 값을 임의의 운용 기본값으로 확정하지 않는다.

## 4. 품질 규칙

- 단순하고 읽기 쉽고 유지보수 가능한 구현을 우선한다.
- 함수와 class는 가능한 한 하나의 책임에 집중시킨다.
- 불필요한 추상화, 조기 최적화와 over-engineering을 피한다.
- 정상 동작뿐 아니라 경계값, 잘못된 입력, timeout, 연결 단절과 fault를 검토한다.
- 변경 위험에 비례하는 unit test, integration test 또는 수동 검증을 수행한다.
- test가 실패하면 원인을 확인하며, test를 통과시키기 위해 요구사항을 약화하지 않는다.
- 실제 hardware를 움직이는 시험 전에는 안전 범위와 정지 방법을 확인한다.

## 5. 문서 관리 규칙

- `README.md`에는 **어떤 프로젝트와 기능을 개발하는지와 현재 작업·구현 현황**을 기록한다.
- `TODO_LIST.md`에는 **작업을 어떤 단계와 순서로 진행하는지와 세부 진행도**를 기록한다.
- `AGENTS.md`에는 **작업자가 어떤 행동 원칙을 따라야 하는지** 기록한다.
- 구현되지 않은 기능을 완료된 기능처럼 문서화하지 않는다.
- 확인할 수 없는 기능, 설정, 성능과 일정을 만들어내지 않는다.
- 기존 문서를 갱신할 수 있으면 같은 목적의 새 문서를 만들지 않는다.
- code와 문서가 충돌하면 실제 구현과 확인된 요구사항을 기준으로 문서를 바로잡는다.
- 새 module을 추가하면 그 목적과 실제 상태를 `README.md`에 반영하고, 진행 상태를 `TODO_LIST.md`에 반영한다.
- 문제를 수정하면 root cause, 해결 방법과 남은 제한을 관련 문서에 기록한다.
- 명칭이나 interface가 바뀌면 code, 설정, test, protocol 예시와 문서를 함께 검색해 동기화한다.

분석, 계획, 검토 또는 요약만을 위한 임시 문서·보고서·메모 파일을 만들지 않는다. 사용자가 요청했거나 실제 산출물에 필요한 파일만 생성한다.

## 6. 언어 규칙

### Programming language

- Python을 기본 programming language로 사용한다.
- 기존 구현, hardware SDK 또는 사용자의 명시적 요청이 있는 경우에만 다른 언어를 사용한다.
- Python best practice와 기존 project coding style을 따른다.

### Communication language

- 사용자 설명, 논의와 사용자용 문서는 한국어를 기본으로 작성한다.
- API, library, framework, protocol keyword와 programming concept에는 필요한 경우 영어를 사용한다.
- 변수, 함수, class, file, module과 API의 이름은 영어를 사용한다.
- 프로젝트에서 정한 용어는 `README.md`의 정의를 따른다.

## 7. 파일 안전 규칙

- 사용자가 명시적으로 요청하지 않은 기존 파일을 삭제하거나 이름을 변경하지 않는다.
- 사용자 작성 내용을 임의로 되돌리거나 덮어쓰지 않는다.
- 관련 없는 파일과 변경사항은 그대로 보존한다.
- 기존 project 구조는 구조 변경이 요청된 경우가 아니면 유지한다.
- 사용하지 않는 파일을 발견하면 제거하지 말고 사용자에게 보고한다.
- 생성할 파일이 실제 산출물인지 확인하고 불필요한 파일은 만들지 않는다.
- 임시 파일이 반드시 필요하면 project 산출물과 분리하고 작업 종료 전에 정리한다.

## 8. 작업 완료 및 보고 규칙

작업은 다음 조건을 모두 만족할 때 완료된 것으로 판단한다.

- 요청된 기능이나 문서가 실제로 완성되었다.
- 구현이 원래 작업 목적과 현재 범위를 충족한다.
- 변경 내용을 검토하고 가능한 수준의 검증을 수행했다.
- `README.md`와 `TODO_LIST.md`가 실제 구현 상태와 일치한다.
- 불필요한 파일, 중복 구현과 확인되지 않은 정보가 추가되지 않았다.
- 요청 범위를 벗어난 기존 파일이 수정되지 않았다.
- 남은 제한, 미정 사항과 hardware 의존성을 숨기지 않았다.

완료 보고에는 다음 내용만 간결하게 포함한다.

- 달성한 결과
- 변경한 파일
- 수행한 검증
- 남은 제한 또는 다음에 필요한 결정
